import type {
  UserConfig,
  DefineWebBuilderCommand,
} from '@growing-web/web-builder-types'

export function defineConfig(config: UserConfig): UserConfig {
  return config
}

export function defineCommand(command: DefineWebBuilderCommand) {
  return command
}
